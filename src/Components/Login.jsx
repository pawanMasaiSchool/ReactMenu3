import styles from "./Common.module.css";

function Login() {
  return (
    <button className={styles.myButton} style={{ background: "orange" }}>
      LOGIN
    </button>
  );
}

export default Login;
