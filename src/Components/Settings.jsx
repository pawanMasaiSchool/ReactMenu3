import styles from "./Common.module.css";

function Settings() {
  return (
    <button className={styles.myButton} style={{ background: "teal" }}>
      SETTINGS
    </button>
  );
}

export default Settings;
